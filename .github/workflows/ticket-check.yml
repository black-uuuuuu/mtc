name: example
on:
  schedule:
    - cron: '*/1 * * * *'  # 每分鐘執行一次
jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check Tickets
        uses: mooyoul/melon-ticket-actions@v1.1.0
        with:
          product-id: 211740
          schedule-id: 100002
          seat-id: 900
      - name: Show message
        run: echo "go"
